<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>index</title>
    <style>
      #wrap {
        width: 100%;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
      }
      input {
        display: none;
      }
      label {
        display: block;
        padding: 1rem 2rem;
        text-align: center;
        background: #74b9ff;
        cursor: pointer;
        border-radius: 0.5rem;
        color: #fff;
        font-size: 1.2rem;
        font-weight: bold;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        transition: 0.3s;
      }
      label:hover {
        transform: scale(1.1);
      }
    </style>
  </head>
  <body>
    <div id="wrap">
      <form>
        <input id="file" type="file" type="text" />
        <label for="file">파일 선택</label>
      </form>
    </div>
  </body>
</html>
